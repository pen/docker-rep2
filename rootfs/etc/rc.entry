#!/bin/sh

yes no | cp -Ri /var/www/.data /ext/data

php /var/www/scripts/ic2.php setup

cron
h2o -c /etc/h2o.conf -m daemon

while true; do
    rm -f /tmp/2chproxy.pid
    sudo -u nobody /usr/local/bin/2chproxy.pl -c /etc/2chproxy.conf
done
